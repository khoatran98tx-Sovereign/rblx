"use client";

export default function AvatarViewer() {
  return (
    <div
      style={{
        width: 250,
        height: 250,
        borderRadius: "16px",
        background: "#333",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "100px",
      }}
    >
      🧑
    </div>
  );
}
